<%= link_to "New Spec", new_spec_path %>
<% @specs.each do |spec| %>
  <h3><%= spec.title.html_safe %></h3>
  <p><%= spec.body.html_safe %></p>
  <%= link_to "Edit Spec", edit_spec_path(spec) %>
  <% if spec != @specs.last %>
  <hr />
  <% end %>
<% end %>