
<h3><%= @spec.title.html_safe %></h3>
<p><%= @spec.body.html_safe %></p>

<%= link_to "Back", :back %>
<% if user_signed_in? %>
	<%= link_to "Edit", edit_spec_path(@spec) %>
	<%= link_to "Delete", spec_path(@spec), method: :delete, data: { confirm: 'Are you sure?'} %>
<% end %>
